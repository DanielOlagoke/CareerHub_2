When saving the files, make sure to organize them correctly for the application to run properly.
Save the Python file inside the main project folder.
Inside that same folder, create a subfolder named “templates” and place the HTML file there.
Then, create another subfolder (commonly named “static”) inside the main folder to store the CSS and JavaScript files.
